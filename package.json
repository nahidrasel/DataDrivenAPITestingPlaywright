{
  "name": "reqres-playwright-api-framework",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "report": "allure generate ./allure-results --clean -o ./allure-report && allure open ./allure-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "allure-playwright": "^2.0.0-beta.20"
  }
}